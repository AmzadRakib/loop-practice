/* target display every elements of an array 
 */

var numbers = [23, 43, 25, 74, 54, 52, 75];
for (var i = 0; i < 7; i++) {
    var number = numbers[i];
    console.log(number);
}

var itemsOnTable = ['mouse', 'money bag', 'sunglass', 'pen', 'braclet', 'mousepad'];
for (var i = 0; i < itemsOnTable.length; i++) {
    var item = itemsOnTable[i];
    console.log(item);
}